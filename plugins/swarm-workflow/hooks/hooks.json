{
  "PostToolUse": [
    {
      "if": "Write|Edit",
      "hooks": [
        {
          "type": "command",
          "command": "${CLAUDE_PLUGIN_ROOT}/hooks/auto-lint.sh",
          "timeout": 30
        }
      ]
    }
  ]
}
